<div class="row padding-1 p-1">
    <div class="col-md-12">
        
        <div class="form-group mb-2 mb20">
            <label for="nombre" class="form-label">{{ __('Nombre') }}</label>
            <input type="text" name="nombre" class="form-control @error('nombre') is-invalid @enderror" value="{{ old('nombre', $catalogo?->nombre) }}" id="nombre" placeholder="Nombre">
            {!! $errors->first('nombre', '<div class="invalid-feedback" role="alert"><strong>:message</strong></div>') !!}
        </div>
        <div class="form-group mb-2 mb20">
            <label for="tipo_catalogo" class="form-label">{{ __('Tipo Catalogo') }}</label>
            <input type="text" name="tipo_catalogo" class="form-control @error('tipo_catalogo') is-invalid @enderror" value="{{ old('tipo_catalogo', $catalogo?->tipo_catalogo) }}" id="tipo_catalogo" placeholder="Tipo Catalogo">
            {!! $errors->first('tipo_catalogo', '<div class="invalid-feedback" role="alert"><strong>:message</strong></div>') !!}
        </div>

    </div>
    <div class="col-md-12 mt20 mt-2">
        <button type="submit" class="btn btn-primary">{{ __('Submit') }}</button>
    </div>
</div>